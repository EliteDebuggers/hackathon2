python script middelware in flask

Guide:

`python3 -m venv .venv`
`source .venv/bin/activate`

`pip install -r requirements.txt`

Project structure

.
├── attacher.py ( simulates sample attacker )
├── client.py ( authenticated user )
├── README.md
├── requirements.txt
└── server.py ( middelware )

1 directory, 6 files